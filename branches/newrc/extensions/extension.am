# Automake machinery common to all extensions

# subcommands
subcommandsdir = $(libexecdir)/dev/subcommands

# extension lib files go here
extlibdir = $(libexecdir)/dev/extensions/$(EXTENSION)

# make sure this stuff gets distributed
EXTRA_DIST = $(extlib_SCRIPTS) $(subcommands_SCRIPTS)

# vim:ft=automake
