# Run 'python setup.py develop' in the given directory; this is most
# useful when the virutalenv has already been activated, e.g., by the
# virtualenv loader
load_pydevelop() {
    local pydir="${1}"

    # note that the unqualified 'pip' here should find its roots in
    # the existing sandbox.  TODO: add a --sandbox option to this loader
    # to specify an alternate sandbox, if necessary
    local pkgcache=`set_up_virtualenv_cache`
    if test -n "${pkgcache}"; then
        pip install --download-cache="${pkgcache}" --editable="$pydir" || exit 1
    else
        pip install --editable="$pydir" || exit 1
    fi
}

